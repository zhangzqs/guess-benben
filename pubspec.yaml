name: guess_benben
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  minio: ^3.5.7
  flutter_easyloading: ^3.0.5
  dio: ^5.7.0
